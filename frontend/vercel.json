{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "ignoreCommand": "if [ '$VERCEL_GIT_COMMIT_REF' != 'main' ]; then exit 0; else git diff --quiet HEAD^ HEAD -- backend/ && exit 0 || exit 1; fi"
}
